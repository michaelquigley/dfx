# 2026-07-07 — NodeCanvas planning arc converged

The NodeCanvas spec (`docs/future/node-canvas.md`) and work order (`docs/future/node-canvas-work-order.md`) completed mercurius review and are ready for implementation. Verdict on the final round: `ready_to_build`, zero concerns.

## State

- Fourteen review rounds (Codex, gpt-5.5), 57 findings walked one at a time with Michael; all accepted, none rejected or deferred. Session `s_fOO4CQSfeIJC`; synopsis at `.mercurius/s_fOO4CQSfeIJC/_synopsis.md` — the audit trail for why each contract detail is the way it is.
- The work order is the implementation contract: five terminus-gated stages, pure-core-first (geometry + gesture state machine, headless-tested) before any rendering. The gesture grammar, arbitration predicates, and navigation semantics are pinned normatively — do not re-derive them; the synopsis records the litigation.
- `mercurius.yaml` is calibrated for this arc (guards cover the planning decisions, the spec's deferred list, the ZoomToFit algorithm, and the selection semantics). If a future review arc starts, re-read and recalibrate `review_focus` first.

## Gotchas the documents encode (verified against pinned cimgui-go v1.5.0 source)

- `imgui.FontSize()` must never be passed to `PushFont` — use `imgui.CurrentStyle().FontSizeBase()`.
- The canvas-level drawlist split must use an owned `imgui.DrawListSplitter`, never `ChannelsSplit` (imgui tables split internally; the convenience path can't nest).
- `fmt.Sprint` of a struct pointer renders `&{fields}`, not an address — the ID scoping rule special-cases pointer kinds via `%p`.
- Material icon merge uses fixed pixel glyph offsets (`fonts.go:48,75`) — icon baselines at reduced detents are a watch item for stage 3.

## Next

Implementation agent executes the work order stage by stage, terminus-gating each stage. Once fully realized: synthesize `docs/current/node-canvas.md`, remove the two `docs/future/` documents (re-synthesizing any still-live deferred content into smaller future docs first), CHANGELOG `FEATURE` entry, AGENTS.md architecture line. The baab migration is a separate follow-on work order in baab's repo.
